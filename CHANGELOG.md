# Changelog

本文档记录项目的所有重要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

---

## [1.0.22] - 2026-08-19

### 修复
- 日期区间选择器（RangeCalendar）事件传递异常：补齐内部 RangeCalendarRoot 的 `@update:model-value` 与 `@update:placeholder` 事件转发，修复选区状态未能同步至响应式变量的问题
- 日期筛选弹窗体验优化：新增底部操作栏（【今天】、【取消】、【确定】按钮），支持单日直接确认生效与区间明确提交

---

## [1.0.21] - 2026-08-18

### 新增
- 数据查询的采集日期支持区间筛选（含起止两端），内置今天 / 近 7 天 / 近 30 天 / 近 3 个月 / 近半年 / 近一年 / 近两年七个快捷范围
- 日期弹层改为双月并排日历，点两下即可自选任意区间；起止都落定后才触发查询
- `/api/shows`、`/api/export` 与清除接口新增 `date_from` / `date_to` 参数，原单日参数 `date` 保留兼容，区间参数同传时优先

### 变更
- 数据查询默认只看当天采集的数据，不再默认加载全部日期
- 日期筛选按钮始终显示真实日期区间（如 `2026-08-12 ~ 2026-08-18`），不再显示「近 7 天」这类预设名

### 修复
- 日历组件周起始日错误：表头原本从星期二开始，现为周一起
- NSIS 安装器钩子编译错误

---

## [1.0.20] - 2026-08-17

### 修复
- 自动更新退出/重启桌面壳时同步停止后端；正式版启动时回收旧版遗留服务，并拒绝连接占用端口的未知服务
- Windows 更新器只有在安装器确实启动后才退出；取消 UAC 或启动失败会保留当前客户端与后端
- 后端随桌面壳监督管道自动退出，避免应用崩溃或 Windows 更新器直接退出时遗留进程占用 8756
- 更新或退出时取消并等待当前采集任务，让 Playwright 完成资源清理后再结束后端
- Windows 安装/卸载前兜底结束旧 `daxi.exe`，首个修复版本也能接管尚不支持新生命周期协议的旧后端
- Windows 后端取消管理员权限要求，自动把 Program Files 中的旧数据库、Cookie 等数据迁入 `LOCALAPPDATA`，确保桌面壳可以可靠回收子进程且升级不丢数据
- 桌面客户端强制单实例，重复打开只唤醒已有窗口，不再让两个客户端争抢或互相结束后端
- 重复项目且没有有效更新时，任务日志明确显示"与已有数据重复，已暂时跳过"
- 打包版导出 Excel 失败（提示 Load failed）：`openpyxl` 未打进后端包，xlsx 导出接口返回 500；已在打包配置中显式收集 `openpyxl` 及其数据文件，CSV 导出不受影响

### 新增
- `/api/health` 增加独立后端构建版本、构建 ID、进程关系与单次启动实例标识；桌面壳同时校验协议、版本和 PID，旧后端无法冒充新后端

---

## [1.0.19] - 2026-08-17

### 修复
- 后端打包请求管理员权限（UAC），修复 Windows 安装到 `C:\Program Files\Daolue` 时报 "Error opening file for writing" 的问题
- 采集浏览器启动失败时，任务日志直接显示真实异常摘要，不再只给笼统的「采集引擎发生异常」
- 人工介入提示不再误报「冰拓连续 N 次未返回距离」，改为描述真实的「识别 / 滑块执行 / 平台校验」三阶段失败

### 新增
- 后端日志落盘到用户数据目录 `logs/server.log`（含完整堆栈），任务失败时可直接取文件排障
- 滑块验证任务日志显示诊断信息：冰拓返回的原始距离、换算后的页面拖动距离、鼠标/滑块实际位移及大麦校验状态码（不暴露 URL、captchaId、图片和令牌）

---

## [1.0.9] - 2026-07-29

### 新增
- 冰拓连续识别失败时自动刷新验证码并按三次预算重试
- 自动过码耗尽后向采集页发布人工介入状态，并弹出浏览器手动验证指引

### 修复
- 统一默认配置、设置保存和运行时的冰拓重试次数
- 人工介入弹窗可稳定关闭，不会因任务状态轮询反复弹出

---

## [1.0.8] - 2026-07-29

### 新增
- 采集详情完成后逐条规范化并即时写入数据库，长任务期间即可查看已完成数据
- 新增历史场次拆分迁移脚本，支持预览、备份和安全迁移
- macOS 升级时自动迁移旧版应用内用户数据到系统用户数据目录

### 修复
- 同一项目跨城市、关键词或翻页重复返回时先去重，避免详情重复请求和重复入库
- 演出记录按真实场次拆分，修复同日多场聚合及日期名称数字误解析问题
- 冰拓余额不再在空凭据或未保存凭据时自动显示
- 冰拓 HTTPS 查询使用随包 CA，修复桌面环境证书路径异常
- 冰拓未返回有效距离时停止付费重试并回退免费本地算法
- 顶部“有新版本”入口可直接打开更新专页并复用已检测到的更新

---

## [0.1.0] - 2024-07-26

### 新增
- **桌面应用形态**：Tauri 跨平台打包,支持 Windows / macOS（Intel + Apple Silicon）
- **数据采集功能**：
  - 大麦演出数据采集,支持城市/分类筛选
  - 猫眼演出数据采集
  - 验证码自动识别(冰拓 1358 平台)+ 手动兜底
  - 阿里 x5、baxia、水果滑块验证码处理
  - 美团 Yoda 验证码处理
  - 详情页富化:场次/票档/地址信息补全
- **数据管理**：
  - SQLite 本地存储
  - 按数据来源/城市/分类/状态/关键词筛选
  - 支持按时间范围清除数据
  - 数据导出为 Excel(.xlsx) 和 CSV 格式
- **用户界面**：
  - Vue 3 + Element Plus + Tailwind CSS
  - 采集页:城市选择器 + 页数控制 + 实时日志
  - 查询页:多条件筛选 + 分页表格 + 排序
  - 设置页:验证码模式切换 + 冰拓账号配置 + 主题色调整
- **自动更新**：集成 Tauri updater,支持应用内检查和安装更新
- **跨平台路径管理**：智能处理打包后的资源路径、数据目录、浏览器位置

### 修复
- 采集页数布局优化:左右区域 50/50 平分,输入框加宽完整显示 placeholder
- CI 流水线修复:移除 Intel Mac 构建以解除 Release 阻塞
- 跨平台打包问题修复:Windows 和 macOS 产物正确生成

### 变更
- 应用改名为 **Daolue（道略）**
- 更换应用图标为道略文旅 logo

### 技术栈
- **前端**: Vue 3.5 + Element Plus 2.14 + Tailwind CSS 3.4 + Vite 6
- **后端**: FastAPI 0.115 + Python 3.11
- **采集**: Playwright 1.49 + Chromium
- **打包**: Tauri 2.11 + PyInstaller
- **数据**: SQLite + Pydantic 2.10

---

## [0.0.1] - 2024-07-初

### 初始版本
- 项目初始化
- 基础爬虫架构搭建
- 验证码处理探索和测试

---

[Unreleased]: https://github.com/jingchen0529/ticket-lens/compare/v1.0.9...HEAD
[1.0.9]: https://github.com/jingchen0529/ticket-lens/compare/v1.0.8...v1.0.9
[1.0.8]: https://github.com/jingchen0529/ticket-lens/compare/v1.0.7...v1.0.8
[0.1.0]: https://github.com/jingchen0529/ticket-lens/releases/tag/v0.1.0
