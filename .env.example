# 环境变量配置示例

本文件列出了所有可用的环境变量配置项。复制并重命名为 `.env` 后填入实际值。

> **注意**: `.env` 文件包含敏感信息,已被 `.gitignore` 排除,不会提交到版本库。

---

## 数据存储

### DAXI_DATA_DIR
**用途**: 指定数据存储目录(SQLite 数据库、cookies、配置文件等)

**默认行为**:
- 未设置时自动选择:
  1. 打包版: 优先使用可执行文件同级 `data/` 目录
  2. 若不可写(如 Windows Program Files): 回退到系统用户目录
     - Windows: `%LOCALAPPDATA%\daxi`
     - macOS: `~/Library/Application Support/daxi`
     - Linux: `~/.local/share/daxi`
  3. 源码运行: `data/`

**示例**:
```bash
# 指定自定义数据目录
DAXI_DATA_DIR=/path/to/custom/data

# Windows 示例
DAXI_DATA_DIR=D:\daxi_data

# macOS/Linux 示例
DAXI_DATA_DIR=/Users/username/Documents/daxi_data
```

**何时使用**:
- 需要将数据存储在特定位置
- 多个安装实例共享数据
- 数据备份和迁移

---

## 浏览器配置

### PLAYWRIGHT_BROWSERS_PATH
**用途**: 指定 Playwright 浏览器(Chromium)的安装路径

**默认行为**:
- 打包版: 自动使用随包分发的 `ms-playwright/` 目录
- 源码运行: 使用 Playwright 默认缓存目录
  - macOS: `~/Library/Caches/ms-playwright`
  - Linux: `~/.cache/ms-playwright`
  - Windows: `%USERPROFILE%\AppData\Local\ms-playwright`

**示例**:
```bash
# 指定自定义浏览器路径(调试用)
PLAYWRIGHT_BROWSERS_PATH=/path/to/custom/browsers

# 使用系统已安装的 Playwright 浏览器
PLAYWRIGHT_BROWSERS_PATH=~/.cache/ms-playwright
```

**何时使用**:
- 开发调试时想使用特定版本的 Chromium
- 多个项目共享浏览器缓存以节省磁盘空间
- **注意**: 打包版一般不需要设置,会自动配置

---

## 应用配置

### DAXI_LOG_LEVEL
**用途**: 设置日志级别

**可选值**: `DEBUG` | `INFO` | `WARNING` | `ERROR`

**默认**: `INFO`

**示例**:
```bash
# 开发时查看详细日志
DAXI_LOG_LEVEL=DEBUG

# 生产环境减少日志输出
DAXI_LOG_LEVEL=WARNING
```

---

### DAXI_BACKEND_PORT
**用途**: 指定后端 API 服务监听端口

**默认**: `8756`

**示例**:
```bash
# 使用自定义端口(需同时修改前端配置)
DAXI_BACKEND_PORT=9000
```

**注意**: 修改此值需要同步修改 `web/frontend/src/api.js` 和 `web/frontend/src-tauri/src/main.rs` 中的端口

---

## 验证码配置

### BINGTOP_USERNAME
**用途**: 冰拓打码平台账号

**示例**:
```bash
BINGTOP_USERNAME=your_email@example.com
```

---

### BINGTOP_PASSWORD
**用途**: 冰拓打码平台密码

**示例**:
```bash
BINGTOP_PASSWORD=your_password
```

---

### BINGTOP_CAPTCHA_TYPE
**用途**: 冰拓验证码类型代码

**默认**: `1358`

**示例**:
```bash
BINGTOP_CAPTCHA_TYPE=1358
```

**注意**: 冰拓配置也可以在应用的"系统设置"页面中配置,优先级高于环境变量

---

## 开发调试

### DEBUG
**用途**: 启用调试模式

**示例**:
```bash
# 启用调试(更详细的错误堆栈、自动重载等)
DEBUG=1
```

---

### PYTEST_ARGS
**用途**: 传递给 pytest 的额外参数

**示例**:
```bash
# 运行测试时显示详细输出
PYTEST_ARGS="-v -s"

# 只运行特定测试
PYTEST_ARGS="-k test_damai"
```

---

## 完整示例

### 开发环境 `.env`
```bash
# 数据存储在项目内
DAXI_DATA_DIR=./data

# 详细日志
DAXI_LOG_LEVEL=DEBUG

# 启用调试模式
DEBUG=1
```

### 生产环境(客户机器)
```bash
# 自定义数据目录
DAXI_DATA_DIR=D:\daxi_data

# 冰拓打码账号(可选,也可在界面配置)
BINGTOP_USERNAME=customer@example.com
BINGTOP_PASSWORD=secure_password_here

# 减少日志输出
DAXI_LOG_LEVEL=WARNING
```

---

## 配置优先级

多种配置方式的优先级(从高到低):

1. **环境变量** (`.env` 或系统环境变量)
2. **应用内设置** (系统设置页面)
3. **配置文件** (`data/configs/default.yaml`)
4. **代码默认值**

---

## 使用方法

### 方式 1: 创建 .env 文件(推荐)

```bash
# 复制示例文件
cp .env.example .env

# 编辑 .env 填入实际值
nano .env  # 或使用任何文本编辑器
```

### 方式 2: 系统环境变量

**macOS/Linux**:
```bash
# 临时设置(当前 shell 会话)
export DAXI_DATA_DIR=/path/to/data

# 永久设置(添加到 ~/.zshrc 或 ~/.bashrc)
echo 'export DAXI_DATA_DIR=/path/to/data' >> ~/.zshrc
source ~/.zshrc
```

**Windows**:
```cmd
# 临时设置(当前命令提示符)
set DAXI_DATA_DIR=D:\daxi_data

# 永久设置(通过系统设置 → 环境变量)
setx DAXI_DATA_DIR "D:\daxi_data"
```

### 方式 3: 启动时传递

```bash
# 运行时传递环境变量
DAXI_DATA_DIR=/tmp/test python -m app.cli serve
```

---

## 安全提示

- **不要提交 `.env` 到版本控制**: 已被 `.gitignore` 排除
- **保护敏感信息**: 尤其是验证码平台账号密码
- **权限控制**: 确保 `.env` 文件只有所有者可读
  ```bash
  chmod 600 .env
  ```
