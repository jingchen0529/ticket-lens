{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "本地桌面壳默认权限：仅核心窗口能力，后端通过本地 HTTP 调用。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "fs:allow-write-file",
    "http:default",
    "shell:allow-open"
  ]
}
